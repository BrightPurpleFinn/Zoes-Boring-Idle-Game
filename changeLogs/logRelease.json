[
  {
    "patchTitle": "Reworked Version workflow",
    "changes": [
      "No longer has bots pushing commits, avoiding scenarios where it would fail PRs because it wouldn't run the workflows, is a manual process",
      "Every time you make a push into release, you would be adding a new log to logRelease.json, which before merging into main, this will be merged into log.json"
    ],
    "patchType": "minor",
    "version": "0.3.0"
  }
]